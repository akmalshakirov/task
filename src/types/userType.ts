export type UserType = {
    id: number;
    firstName: string;
    lastName: string;
    gender: string;
    phone: string;
    email: string;
    username: string;
    role: string;
};

export type TableColumns = {
    key: string;
    header: string;
};
